Bootstrap: docker
FROM: bioconductor/release_core2

%labels
  Version v1.0

%help
  Singularity image for the variant calling pipeline (R 3.5.1 + all packages)



%post

  R --slave -e "install.packages('tools')"
  R --slave -e "install.packages('tidyverse')"
  R --slave -e "install.packages('grid')"


%environment

%test
  R --version
